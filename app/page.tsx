import { EmptyState } from '@sovereignfs/ui';

export default function LedgerIndexPage() {
  return (
    <EmptyState
      heading="Ledger is under construction"
      description="The monthly overview dashboard lands in a later task — see roadmap.md."
    />
  );
}
